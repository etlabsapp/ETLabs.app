{
  "name": "etlabs-site",
  "private": true,
  "scripts": {
    "install:marketing": "npm install --prefix apps/marketing",
    "dev": "npm run dev --prefix apps/marketing",
    "build": "npm run build --prefix apps/marketing",
    "start": "npm run start --prefix apps/marketing",
    "lint": "npm run lint --prefix apps/marketing"
  }
}
